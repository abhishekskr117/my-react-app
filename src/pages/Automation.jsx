import { Box, Typography } from '@mui/material';

const Automation = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">Automation</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>
        Configure automated processes for the farm.
      </Typography>
    </Box>
  );
};

export default Automation;